declare class CMYKColor {
    /** Read-write. The black color value(as percent in [0.0..100.00]). */
    black: number
    /** Read-write. The cyan color value(as percent in [0.0..100.00]). */
    cyan: number
    /** Read-write. The meganta color value(as percent in [0.0..100.00]). */
    magenta: number
    /** The class name of the referenced CMYKColor object. */
    readonly typename: number
    /** Read-write. The yellow color value(as percent in [0.0..100.00]). */
    yellow: number
}